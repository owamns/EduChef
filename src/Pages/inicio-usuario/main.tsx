import React from "react";
import ReactDOM from "react-dom/client";
import InicioUsuario from "./inicio-usuario.tsx";
import "bootstrap/dist/css/bootstrap.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
	<React.StrictMode>
		<InicioUsuario />
	</React.StrictMode>
);
